{% include 'default/_form.html.twig' with {
    page_title: 'title_project_delete',
    page_title_param: {'%id%': project.id},
    attr: {
        method: 'DELETE',
        action: url('project_delete', {'id': project.id}),
    },
    form: form,
    submit_label: 'action_delete'|trans,
    back_to_list_path: 'project_index',
} %}